INSERT INTO carry1st_platform.refined.CRM__CAMPAIGNS 

-- SELECT
--     customer_id,
--     last_recipient,
--     push_token,
--     variant_group,
--     campaign_name,
--     send_date
-- FROM  carry1st_platform.refined.CRM__CAMPAIGNS
